import { themeMaterial } from 'ag-grid-community';

export const baseTableTheme = themeMaterial.withParams({
  wrapperBorder: true,
  headerRowBorder: true,
  headerColumnBorder: true,
  rowBorder: true,
  columnBorder: true,
  fontFamily: 'Lato, sans-serif',
});
